# ArduPilot 断点续飞功能实现文档

## 一、功能概述

断点续飞功能允许无人机在执行AUTO任务过程中被中断（如切换到GUIDE模式、降落等）后，能够恢复任务并从中断点继续执行，而不是直接飞向下一个航点。

### 功能特点
- 飞机降落后可重新起飞继续任务
- 起飞后先飞向中断时的位置（断点）
- 到达断点后再飞向原计划航点
- 保持任务的连续性和完整性

---

## 二、相关参数

### 2.1 MIS_RESUME_MODE
**功能**: 控制任务中断后的恢复行为

**可选值**:
- `0`: Direct to Target - 直接飞向目标航点（默认）
- `1`: Return to strict track - 返回原轨迹线后继续
- `2`: Breakpoint resume - 断点恢复模式 ⭐

**使用**: 需要设置为 `2` 才能启用断点续飞

### 2.2 MIS_RESTART
**功能**: 控制任务是否从头开始

**可选值**:
- `0`: Resume from last command - 从上次命令恢复
- `1`: Restart from beginning - 从头开始

**使用**: 需要设置为 `0` 才能保持断点

---

## 三、实现架构

### 3.1 核心数据结构

```cpp
// AP_Mission.h 中的关键成员变量
class AP_Mission {
    Location _breakpoint;           // 断点位置（包含经纬度和高度）
    uint16_t _breakpoint_nav_cmd_index; // 断点对应的航点索引
    bool _breakpoint_valid;         // 断点是否有效
    
    struct mission_flags {
        bool resuming_mission : 1;  // 标识正在恢复任务
    } _flags;
    
    uint8_t _return_track_phase;    // 返回轨迹的阶段
    // 0 = 未进入, 1 = 起飞中, 2 = 飞往断点, 3 = 飞往原航点
};
```

### 3.2 虚拟命令索引

为避免临时生成的起飞和断点命令干扰正常任务流程，使用特殊索引值：

```cpp
#define AP_MISSION_CMD_INDEX_NONE 0xFFFF
#define VIRTUAL_TAKEOFF_INDEX     (AP_MISSION_CMD_INDEX_NONE - 1)  // 0xFFFE
#define VIRTUAL_PROJECTION_INDEX  (AP_MISSION_CMD_INDEX_NONE - 2)  // 0xFFFD
```

---

## 四、核心流程

### 4.1 断点保存流程

**触发时机**: 退出AUTO模式时（ModeAuto::exit()）

**执行步骤**:
1. 检查 `MIS_RESUME_MODE` 是否为 2
2. 获取当前位置 `copter.current_loc`
3. 转换高度到 `ABOVE_HOME` 参考系
4. 调用 `mission.set_breakpoint()` 保存断点

**关键代码位置**: `ArduCopter/mode_auto.cpp::exit()`

```cpp
void ModeAuto::exit() {
    if (copter.mission.get_resume_mode() == AP_Mission::Resume_Mode::RESUME_BREAKPOINT) {
        Location current_loc_copy = copter.current_loc;
        if (!current_loc_copy.change_alt_frame(Location::AltFrame::ABOVE_HOME)) {
            current_loc_copy.set_alt_cm(0, Location::AltFrame::ABOVE_HOME);
        }
        copter.mission.set_breakpoint(current_loc_copy);
    }
}
```

### 4.2 断点设置逻辑

**功能**: 保存断点位置和对应的航点索引

**关键逻辑** (`AP_Mission::set_breakpoint()`):
1. 获取当前导航命令索引
2. 向前搜索最后一个有效航点（跳过 LAND、DO_LAND_START 等命令）
3. 保存断点位置和索引
4. 设置 `_breakpoint_valid = true`

**代码位置**: `libraries/AP_Mission/AP_Mission.cpp::set_breakpoint()`

### 4.3 任务恢复流程

**触发时机**: 进入AUTO模式时（ModeAuto::init()）

**安全检查绕过**:
原本需要任务以起飞命令开始才能进入AUTO，断点续飞时放宽限制：

```cpp
bool ModeAuto::init(bool ignore_checks) {
    if (copter.mission.get_resume_mode() == AP_Mission::Resume_Mode::RESUME_BREAKPOINT &&
        copter.mission.is_breakpoint_valid()) {
        // 允许进入，即使任务不以起飞命令开始
    } else if (!copter.mission.starts_with_takeoff_cmd()) {
        return false; // 正常情况需要起飞命令
    }
}
```

**代码位置**: `ArduCopter/mode_auto.cpp::init()`

### 4.4 断点恢复执行

**核心函数**: `AP_Mission::resume()`

**执行步骤**:

1. **检查断点有效性**
```cpp
if (_resume_mode == Resume_Mode::RESUME_BREAKPOINT && _breakpoint_valid) {
    // 进入断点恢复逻辑
}
```

2. **生成虚拟起飞命令**
```cpp
Mission_Command takeoff_cmd;
takeoff_cmd.id = MAV_CMD_NAV_TAKEOFF;
takeoff_cmd.index = VIRTUAL_TAKEOFF_INDEX; // 0xFFFE
takeoff_cmd.content.location = _breakpoint;
takeoff_cmd.content.location.set_alt_cm(150, Location::AltFrame::ABOVE_HOME);
```

3. **设置恢复状态**
```cpp
_flags.resuming_mission = true;
_return_track_phase = 1; // 阶段1：起飞中
```

4. **加载断点航点**
```cpp
read_cmd_from_storage(_breakpoint_nav_cmd_index, _nav_cmd);
```

**代码位置**: `libraries/AP_Mission/AP_Mission.cpp::resume()`

### 4.5 多阶段导航

**核心函数**: `AP_Mission::update()`

**阶段控制**:

```
阶段 1: 起飞中 (_return_track_phase = 1)
  ↓ 起飞完成
阶段 2: 飞往断点 (_return_track_phase = 2)
  ↓ 到达断点
阶段 3: 飞往原航点 (_return_track_phase = 3)
  ↓ 到达航点
恢复正常任务流程
```

**关键代码逻辑**:

```cpp
// 阶段1完成：起飞完成
if (_return_track_phase == 1 && _nav_cmd.index == VIRTUAL_TAKEOFF_INDEX) {
    // 生成飞往断点的命令
    Mission_Command projection_cmd;
    projection_cmd.id = MAV_CMD_NAV_WAYPOINT;
    projection_cmd.index = VIRTUAL_PROJECTION_INDEX; // 0xFFFD
    projection_cmd.content.location = _breakpoint;
    
    _nav_cmd = projection_cmd;
    _return_track_phase = 2; // 进入阶段2
}

// 阶段2完成：到达断点
if (_return_track_phase == 2 && _nav_cmd.index == VIRTUAL_PROJECTION_INDEX) {
    // 加载原目标航点
    read_cmd_from_storage(_breakpoint_nav_cmd_index, _nav_cmd);
    _return_track_phase = 3; // 进入阶段3
}

// 阶段3完成：到达原航点
if (_return_track_phase == 3) {
    _flags.resuming_mission = false;
    _return_track_phase = 0; // 恢复完成
}
```

**代码位置**: `libraries/AP_Mission/AP_Mission.cpp::update()`

### 4.6 虚拟索引过滤

防止虚拟命令干扰正常任务逻辑：

```cpp
// 不记录虚拟命令到航点历史
if (_nav_cmd.index < AP_MISSION_CMD_INDEX_NONE - 10) {
    add_to_wp_history(_nav_cmd.index);
}

// 不对虚拟命令执行"返回中断航点"逻辑
if (_nav_cmd.index >= AP_MISSION_CMD_INDEX_NONE - 10) {
    return; // 跳过虚拟命令
}
```

---

## 五、解锁/上锁逻辑

### 5.1 上锁时保护断点

**问题**: 默认的 `disarm()` 会调用 `mission.reset()`，清除所有任务状态

**解决方案**: 在断点续飞模式下，使用轻量级清理

```cpp
bool AP_Arming::disarm(const AP_Arming::Method method, bool do_disarm_checks) {
    if (copter.mission.get_resume_mode() == AP_Mission::Resume_Mode::RESUME_BREAKPOINT &&
        copter.mission.is_breakpoint_valid()) {
        // 只清除导航命令索引，保留断点信息
        copter.mission.clear_nav_cmd_index();
        copter.mission.stop();
    } else {
        // 正常情况下完全重置
        copter.mission.reset();
    }
}
```

**代码位置**: `ArduCopter/AP_Arming.cpp::disarm()`

### 5.2 清除导航索引

**功能**: 清除当前命令但保留断点

```cpp
void AP_Mission::clear_nav_cmd_index() {
    _nav_cmd.index = AP_MISSION_CMD_INDEX_NONE;
    _nav_cmd.id = 0;
    // 注意：不清除 _breakpoint_valid
}
```

**代码位置**: `libraries/AP_Mission/AP_Mission.cpp::clear_nav_cmd_index()`

---

## 六、涉及的文件和修改

### 6.1 AP_Mission.cpp（任务管理核心）

**修改点**:
- `resume()`: 实现断点恢复逻辑，生成虚拟起飞命令
- `set_breakpoint()`: 保存断点位置和索引，处理高度坐标系
- `update()`: 实现多阶段导航（起飞→断点→航点）
- `clear_nav_cmd_index()`: 新增函数，清除导航索引但保留断点
- 虚拟索引过滤：防止临时命令干扰任务流程

**文件路径**: `/home/liu/ardupilot/libraries/AP_Mission/AP_Mission.cpp`

### 6.2 AP_Mission.h（接口定义）

**修改点**:
- 新增 `clear_nav_cmd_index()` 函数声明
- 新增 `is_resuming_mission()` 函数声明（用于外部查询状态）

**文件路径**: `/home/liu/ardupilot/libraries/AP_Mission/AP_Mission.h`

### 6.3 mode_auto.cpp（AUTO模式控制）

**修改点**:
- `init()`: 放宽进入AUTO模式的限制，允许断点续飞时无起飞命令
- `exit()`: 保存断点，使用 `current_loc` 并转换为 `ABOVE_HOME` 坐标系
- 移除 `verify_land()` 中的断点保存逻辑（避免高度归零）

**文件路径**: `/home/liu/ardupilot/ArduCopter/mode_auto.cpp`

### 6.4 AP_Arming.cpp（解锁控制）

**修改点**:
- `disarm()`: 在断点续飞模式下，使用 `clear_nav_cmd_index()` 和 `stop()` 代替 `reset()`，保护断点数据

**文件路径**: `/home/liu/ardupilot/ArduCopter/AP_Arming.cpp`

### 6.5 GCS_Mavlink.cpp（地面站通信）

**修改点**:
- `handle_MAV_CMD_NAV_TAKEOFF()`: 移除冗余的 `start_or_resume()` 调用，让 `ModeAuto::run()` 循环自然处理任务启动

**文件路径**: `/home/liu/ardupilot/ArduCopter/GCS_Mavlink.cpp`

---

## 七、完整执行流程示例

### 场景：AUTO → GUIDE → LAND → AUTO（断点续飞）

#### 第一阶段：正常任务执行

1. **起飞并执行任务**
   ```
   [用户] 设置 MIS_RESUME_MODE=2, MIS_RESTART=0
   [用户] 切换到 AUTO 模式
   [系统] ModeAuto::init() → mission.start() → 起飞到航点1
   [系统] 飞向航点2
   [系统] 飞向航点3（飞行中）
   ```

2. **中断任务**
   ```
   [用户] 切换到 GUIDE 模式
   [系统] ModeAuto::exit()
          → current_loc = 当前位置（假设：lat=X, lon=Y, alt=151cm, frame=ABOVE_HOME）
          → mission.set_breakpoint(current_loc)
          → _breakpoint 保存为 {lat=X, lon=Y, alt=151, frame=ABOVE_HOME}
          → _breakpoint_nav_cmd_index = 3
          → _breakpoint_valid = true
   ```

3. **降落**
   ```
   [用户] 切换到 LAND 模式
   [系统] 飞机降落，land_complete=true
   [系统] 自动上锁 → AP_Arming::disarm()
          → 检测到 MIS_RESUME_MODE=2 且 _breakpoint_valid=true
          → 调用 clear_nav_cmd_index()（_nav_cmd.index = 0xFFFF）
          → 调用 stop()（_flags.state = MISSION_STOPPED）
          → 断点信息被保留！
   ```

#### 第二阶段：断点续飞

4. **重新解锁并进入AUTO**
   ```
   [用户] 解锁并切换到 AUTO 模式
   [系统] ModeAuto::init()
          → 检查 starts_with_takeoff_cmd()? 可能为 false
          → 检查 MIS_RESUME_MODE=2 且 _breakpoint_valid=true
          → 允许进入 AUTO 模式（绕过起飞命令检查）
          → 设置 waiting_to_start = true
   ```

5. **阶段1：虚拟起飞**
   ```
   [系统] ModeAuto::run() → mission.start_or_resume()
          → mission.resume()
          → 检测到 _breakpoint_valid=true
          → 生成虚拟起飞命令:
             {
               id = MAV_CMD_NAV_TAKEOFF,
               index = 0xFFFE (VIRTUAL_TAKEOFF_INDEX),
               location = {lat=X, lon=Y, alt=150cm, frame=ABOVE_HOME}
             }
          → _nav_cmd = 虚拟起飞命令
          → _flags.resuming_mission = true
          → _return_track_phase = 1
          → 开始执行起飞
   
   [系统] 飞机起飞到 1.5m 高度
   [系统] verify_takeoff() → 完成
   [系统] mission.advance_current_nav_cmd() → mission.update()
   ```

6. **阶段2：飞往断点**
   ```
   [系统] mission.update()
          → _return_track_phase == 1 且 _nav_cmd.index == 0xFFFE
          → 生成飞往断点的命令:
             {
               id = MAV_CMD_NAV_WAYPOINT,
               index = 0xFFFD (VIRTUAL_PROJECTION_INDEX),
               location = _breakpoint {lat=X, lon=Y, alt=151cm, frame=ABOVE_HOME}
             }
          → _nav_cmd = 虚拟断点命令
          → _return_track_phase = 2
          → 开始飞往断点 (X, Y, 151cm)
   
   [系统] 飞机到达断点位置
   [系统] verify_nav_wp() → 完成
   [系统] mission.advance_current_nav_cmd() → mission.update()
   ```

7. **阶段3：飞往原目标航点**
   ```
   [系统] mission.update()
          → _return_track_phase == 2 且 _nav_cmd.index == 0xFFFD
          → read_cmd_from_storage(_breakpoint_nav_cmd_index=3, _nav_cmd)
          → _nav_cmd = 航点3的实际命令
          → _return_track_phase = 3
          → 开始飞往航点3
   
   [系统] 飞机到达航点3
   [系统] verify_nav_wp() → 完成
   [系统] mission.advance_current_nav_cmd() → mission.update()
   ```

8. **恢复正常任务流程**
   ```
   [系统] mission.update()
          → _return_track_phase == 3
          → _flags.resuming_mission = false
          → _return_track_phase = 0
          → 断点恢复完成
   
   [系统] 继续执行后续航点（航点4, 5, ...）
   [系统] 任务完成 → "Mission complete"
   ```

---

## 八、关键技术细节

### 8.1 高度坐标系处理

**问题**: 不同阶段使用不同的高度参考系

**解决方案**:
- 断点保存时统一转换为 `ABOVE_HOME`
- 虚拟起飞命令使用 `ABOVE_HOME`
- 确保高度一致性

```cpp
Location current_loc_copy = copter.current_loc;
if (!current_loc_copy.change_alt_frame(Location::AltFrame::ABOVE_HOME)) {
    current_loc_copy.set_alt_cm(0, Location::AltFrame::ABOVE_HOME);
}
```

### 8.2 降落时断点保存时机

**错误做法**: 在 `verify_land()` 中保存断点
- 问题：此时飞机已经着地，高度接近0，导致断点高度错误

**正确做法**: 在 `exit()` 中保存断点
- 优点：在切换模式时保存，飞机还在空中，高度正确

### 8.3 任务状态清理策略

**问题**: `mission.reset()` 会清除所有状态，包括断点

**解决方案**: 分层清理
- 正常模式：`reset()` - 完全清除
- 断点续飞模式：`clear_nav_cmd_index()` + `stop()` - 保留断点

### 8.4 虚拟索引的必要性

**问题**: 临时生成的起飞和断点命令会被加入航点历史，触发不必要的逻辑

**解决方案**: 使用特殊索引值（0xFFFE, 0xFFFD）
- 在关键位置过滤这些索引
- 不加入航点历史
- 不触发"返回中断航点"逻辑

---

## 九、调试方法

### 9.1 关键日志点

在调试时可以添加以下日志（开发完成后已移除）：

```cpp
// 断点保存
GCS_SEND_TEXT(MAV_SEVERITY_INFO, "Breakpoint set at WP %u, alt=%d", 
              index, (int)loc.alt);

// 断点恢复
GCS_SEND_TEXT(MAV_SEVERITY_INFO, "Resume via breakpoint - phase=%u", 
              _return_track_phase);

// 阶段切换
GCS_SEND_TEXT(MAV_SEVERITY_INFO, "Phase transition: %u -> %u", 
              old_phase, new_phase);
```

### 9.2 参数验证

```bash
# 检查参数设置
param show MIS_RESUME_MODE  # 应该为 2
param show MIS_RESTART      # 应该为 0
```

### 9.3 状态验证

关键状态变量：
- `_breakpoint_valid`: 断点是否有效
- `_breakpoint_nav_cmd_index`: 断点航点索引
- `_flags.resuming_mission`: 是否正在恢复
- `_return_track_phase`: 当前恢复阶段 (0-3)

---

## 十、注意事项与限制

### 10.1 使用条件
1. 必须设置 `MIS_RESUME_MODE = 2`
2. 必须设置 `MIS_RESTART = 0`
3. 任务必须包含有效的导航航点
4. 断点不能在 LAND 或 DO_LAND_START 命令上

### 10.2 安全注意事项
1. 确保起飞点附近没有障碍物
2. 断点高度必须高于安全高度
3. 电池电量足够完成剩余任务
4. GPS定位良好

### 10.3 已知限制
1. 不支持跳转命令（JUMP）的断点恢复
2. 断点信息在重启后会丢失
3. 仅支持 Copter 平台

---

## 十一、测试场景

### 11.1 基本功能测试

**测试步骤**:
1. 设置参数：`MIS_RESUME_MODE=2`, `MIS_RESTART=0`
2. 上传包含4-5个航点的任务
3. 切换到AUTO，起飞并飞向第2个航点
4. 在飞行中途切换到GUIDE模式
5. 切换到LAND模式降落
6. 降落后上锁
7. 重新解锁，切换到AUTO模式
8. 观察：飞机应该起飞 → 飞向中断点 → 飞向原航点 → 继续任务

**预期结果**: 
- 飞机能够成功起飞
- 先飞向断点位置
- 然后继续执行原任务

### 11.2 边界条件测试

**测试场景**:
- 在第一个航点就中断
- 在最后一个航点之前中断
- 在降落命令之前中断
- 切换到不同模式（LOITER, RTL）后再切回AUTO

---

## 十二、总结

断点续飞功能通过精心设计的状态管理和虚拟命令机制，实现了任务的无缝恢复。核心思想是：

1. **保存状态**: 在退出AUTO模式时保存当前位置和航点索引
2. **状态保护**: 在上锁时不清除断点信息
3. **分阶段恢复**: 起飞 → 断点 → 航点 → 正常任务
4. **虚拟命令隔离**: 使用特殊索引避免干扰正常流程

该功能特别适用于需要中途暂停、更换电池、或手动调整位置后继续任务的应用场景。

---

**文档版本**: 1.0  
**最后更新**: 2025-11-12  
**适用版本**: ArduPilot (自定义修改版)

