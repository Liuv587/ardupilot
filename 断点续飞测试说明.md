# 断点续飞功能测试说明

## 一、功能说明

无人机在执行AUTO任务时被中断（切换模式、降落等），再次切换到AUTO模式后，会先起飞飞到中断点，然后继续执行原任务。

**应用场景**：中途暂停任务、更换电池后继续任务等。

---

## 二、参数配置

测试前必须设置以下参数：

| 参数 | 值 | 说明 |
|------|-----|------|
| `MIS_RESUME_MODE` | `2` | 启用断点续飞模式 |
| `MIS_RESTART` | `0` | 允许从上次位置恢复 |

**设置方法**：在地面站参数列表中搜索并修改这两个参数。

---

## 三、测试步骤

### 基本功能测试

1. **准备任务**
   - 上传包含 4-5 个航点的简单任务
   - 确保第一个命令是 `TAKEOFF`（起飞）

2. **第一次飞行**
   ```
   解锁 → 切换到 AUTO 模式 → 起飞 → 飞向航点2
   ```
   - 当飞到第 2 或第 3 个航点之间时，切换到 `GUIDE` 模式
   - 切换到 `LAND` 模式，等待降落
   - 降落后自动上锁

3. **第二次飞行（断点续飞）**
   ```
   解锁 → 切换到 AUTO 模式
   ```
   - 飞机应该自动起飞
   - 飞往中断时的位置（断点）
   - 到达断点后，飞往原目标航点
   - 继续执行后续任务直到完成

---

## 四、预期结果

### ✅ 正确行为

| 阶段 | 预期现象 |
|------|---------|
| **第一次飞行中断** | 切换模式后，飞机正常响应，降落后上锁 |
| **第二次解锁** | 能够成功解锁，切换到 AUTO 模式不报错 |
| **起飞** | 飞机起飞到 1.5m 高度 |
| **飞往断点** | 飞机飞向第一次中断时的位置 |
| **到达断点** | GCS 提示到达航点，开始飞向原目标航点 |
| **继续任务** | 按顺序执行剩余航点，最终提示"任务完成" |

### ❌ 错误行为（需要报告）

- 第二次无法解锁或进入 AUTO 模式
- 起飞后直接飞往下一个航点（跳过断点）
- 飞机不起飞，直接提示"任务完成"
- 飞到错误的位置

---

## 五、测试场景

### 场景1：中途中断（基本场景）
- **中断位置**：航点 2-3 之间
- **中断方式**：GUIDE → LAND
- **预期**：起飞 → 断点 → 航点3 → 航点4 → 完成

### 场景2：多次中断
- **操作**：中断后恢复，飞行一段距离再次中断
- **预期**：每次都能从最新的断点恢复

### 场景3：不同模式切换
- **中断方式**：切换到 LOITER → LAND
- **预期**：与 GUIDE 模式相同，能够正常恢复

### 场景4：接近终点中断
- **中断位置**：最后一个航点之前
- **预期**：恢复后完成剩余航点

---

## 六、常见问题

### Q1: 提示"任务必须以起飞命令开始"
**原因**：参数未正确设置  
**解决**：检查 `MIS_RESUME_MODE=2` 和 `MIS_RESTART=0`

### Q2: 起飞后直接飞往下一个航点
**原因**：断点信息丢失或代码未正确实现  
**解决**：检查第一次中断时是否成功降落并上锁

### Q3: 飞机不起飞，提示"任务完成"
**原因**：任务状态错误  
**解决**：重新上传任务，重启飞控

### Q4: 断点位置不正确
**原因**：GPS 漂移或高度参考系问题  
**解决**：确保 GPS 信号良好，等待 GPS 定位稳定后再测试

---

## 七、测试检查清单

测试前：
- [ ] 参数 `MIS_RESUME_MODE = 2`
- [ ] 参数 `MIS_RESTART = 0`
- [ ] GPS 信号良好（卫星数 ≥ 10）
- [ ] 电池电量充足（≥ 50%）
- [ ] 任务已上传并验证

测试中：
- [ ] 第一次飞行能正常起飞
- [ ] 切换模式无报错
- [ ] 降落过程正常
- [ ] 降落后自动上锁
- [ ] 第二次能成功解锁
- [ ] 第二次能进入 AUTO 模式
- [ ] 能够自动起飞
- [ ] 飞往断点位置
- [ ] 到达断点后继续任务
- [ ] 任务正常完成

测试后：
- [ ] 记录测试日志
- [ ] 保存飞行轨迹
- [ ] 记录任何异常现象

---

## 八、注意事项

1. **安全第一**
   - 在空旷场地测试
   - 随时准备切换到手动模式
   - 确保有足够电量

2. **环境要求**
   - GPS 信号良好
   - 无磁干扰
   - 风速 < 5 m/s

3. **数据记录**
   - 保存测试日志文件
   - 记录 GCS 提示信息
   - 截图关键步骤

4. **重启测试**
   - 断点信息在飞控重启后会丢失
   - 每次测试需要完整执行一次中断-恢复流程

---

## 九、快速测试（5分钟）

如果时间有限，可以使用仿真器快速验证：

```bash
# 启动仿真
sim_vehicle.py -v ArduCopter

# 在 MAVProxy 中执行
param set MIS_RESUME_MODE 2
param set MIS_RESTART 0
wp load test_mission.txt  # 加载测试任务
mode AUTO                 # 起飞
mode GUIDE                # 中断（飞行中）
mode LAND                 # 降落
# 等待降落完成
mode AUTO                 # 断点续飞
```

观察飞行轨迹是否符合预期：起飞 → 断点 → 原航点 → 继续任务。

---

**文档版本**: 1.0  
**最后更新**: 2025-11-12  
**测试难度**: ⭐⭐☆☆☆（中等）  
**预计测试时间**: 15-20 分钟

