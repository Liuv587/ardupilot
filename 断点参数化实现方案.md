# 断点信息参数化实现方案

## 一、方案概述

将断点信息从普通成员变量改为参数（AP_Param），实现断点信息持久化存储，飞控重启后可恢复。

---

## 二、需要保存的数据

```cpp
// 当前实现（内存变量，重启丢失）
Location _breakpoint_loc;           // 断点位置（经纬度、高度）
bool _breakpoint_valid;             // 断点是否有效
Mission_Command _breakpoint_nav_cmd; // 断点导航命令（主要用索引）
```

**需要持久化的字段**：
1. 断点经度 (int32_t)
2. 断点纬度 (int32_t)
3. 断点高度 (int32_t)
4. 高度参考系 (uint8_t)
5. 断点航点索引 (uint16_t)
6. 断点有效标志 (bool/uint8_t)

---

## 三、实现方案（推荐）

### 方案 A: 使用独立参数 ⭐

#### 3.1 修改 AP_Mission.h

在参数声明区域添加新参数：

```cpp
// 文件: libraries/AP_Mission/AP_Mission.h
// 位置: 第 930-935 行附近（在现有参数定义后）

class AP_Mission {
    // ... 其他代码 ...

    // 现有参数
    AP_Int16                _cmd_total;
    AP_Int16                _options;
    AP_Int8                 _restart;
    AP_Int8                 _resume_mode;

    // 新增：断点参数（持久化）
    AP_Int32                _breakpoint_lat;      // 断点纬度 (1e7)
    AP_Int32                _breakpoint_lon;      // 断点经度 (1e7)
    AP_Int32                _breakpoint_alt;      // 断点高度 (厘米)
    AP_Int8                 _breakpoint_alt_frame; // 高度参考系 (0-5)
    AP_Int16                _breakpoint_nav_index; // 断点航点索引
    AP_Int8                 _breakpoint_valid;     // 断点有效标志 (0/1)

    // 保留的内存变量（运行时使用）
    Location                _breakpoint_loc;       // 从参数加载到这里
    Mission_Command         _breakpoint_nav_cmd;
    // ... 其他代码 ...
};
```

#### 3.2 修改 AP_Mission.cpp - 参数表定义

在参数表中添加新参数定义：

```cpp
// 文件: libraries/AP_Mission/AP_Mission.cpp
// 位置: 第 30-56 行，AP_GROUPINFO 定义区域

const AP_Param::GroupInfo AP_Mission::var_info[] = {
    // 现有参数（索引 0-3 已使用）
    AP_GROUPINFO_FLAGS("TOTAL",  0, AP_Mission, _cmd_total, 0, AP_PARAM_FLAG_INTERNAL_USE_ONLY),
    AP_GROUPINFO("RESTART",  1, AP_Mission, _restart, AP_MISSION_RESTART_DEFAULT),
    AP_GROUPINFO("OPTIONS",  2, AP_Mission, _options, AP_MISSION_OPTIONS_DEFAULT),
    AP_GROUPINFO("RESUME_MODE", 3, AP_Mission, _resume_mode, 0),

    // 新增：断点参数（索引 4-9）
    // @Param: BP_LAT
    // @DisplayName: Breakpoint Latitude
    // @Description: Latitude of breakpoint location in degrees * 1e7. Set by system automatically.
    // @Range: -900000000 900000000
    // @User: Advanced
    // @ReadOnly: True
    AP_GROUPINFO("BP_LAT", 4, AP_Mission, _breakpoint_lat, 0),

    // @Param: BP_LON
    // @DisplayName: Breakpoint Longitude
    // @Description: Longitude of breakpoint location in degrees * 1e7. Set by system automatically.
    // @Range: -1800000000 1800000000
    // @User: Advanced
    // @ReadOnly: True
    AP_GROUPINFO("BP_LON", 5, AP_Mission, _breakpoint_lon, 0),

    // @Param: BP_ALT
    // @DisplayName: Breakpoint Altitude
    // @Description: Altitude of breakpoint in centimeters. Set by system automatically.
    // @Units: cm
    // @User: Advanced
    // @ReadOnly: True
    AP_GROUPINFO("BP_ALT", 6, AP_Mission, _breakpoint_alt, 0),

    // @Param: BP_FRAME
    // @DisplayName: Breakpoint Altitude Frame
    // @Description: Altitude reference frame for breakpoint (0=Absolute, 1=Above Home, etc). Set by system automatically.
    // @Values: 0:Absolute, 1:Above Home, 2:Above Origin, 3:Above Terrain, 4:Above Terrain (ignore home)
    // @User: Advanced
    // @ReadOnly: True
    AP_GROUPINFO("BP_FRAME", 7, AP_Mission, _breakpoint_alt_frame, 0),

    // @Param: BP_INDEX
    // @DisplayName: Breakpoint Waypoint Index
    // @Description: Mission waypoint index at breakpoint. Set by system automatically.
    // @Range: 0 65535
    // @User: Advanced
    // @ReadOnly: True
    AP_GROUPINFO("BP_INDEX", 8, AP_Mission, _breakpoint_nav_index, 0),

    // @Param: BP_VALID
    // @DisplayName: Breakpoint Valid Flag
    // @Description: Whether breakpoint data is valid (0=invalid, 1=valid). Set by system automatically.
    // @Values: 0:Invalid, 1:Valid
    // @User: Advanced
    // @ReadOnly: True
    AP_GROUPINFO("BP_VALID", 9, AP_Mission, _breakpoint_valid, 0),

    AP_GROUPEND
};
```

#### 3.3 修改 set_breakpoint() - 保存到参数

```cpp
// 文件: libraries/AP_Mission/AP_Mission.cpp
// 位置: set_breakpoint() 函数

void AP_Mission::set_breakpoint(const Location& loc)
{
    // 原有逻辑：查找有效航点索引
    uint16_t target_index = _nav_cmd.index;
    if (target_index != AP_MISSION_CMD_INDEX_NONE) {
        Mission_Command temp_cmd;
        while (target_index > 0) {
            if (read_cmd_from_storage(target_index, temp_cmd)) {
                if (temp_cmd.id != MAV_CMD_NAV_LAND && 
                    temp_cmd.id != MAV_CMD_DO_LAND_START) {
                    break;
                }
            }
            target_index--;
        }
    }

    // 保存断点位置到内存变量
    _breakpoint_loc = loc;
    _breakpoint_nav_cmd = _nav_cmd;
    _breakpoint_nav_cmd.index = target_index;

    // ✨ 新增：保存到参数（持久化）
    _breakpoint_lat.set_and_save(loc.lat);
    _breakpoint_lon.set_and_save(loc.lng);
    _breakpoint_alt.set_and_save(loc.alt);
    _breakpoint_alt_frame.set_and_save((int8_t)loc.get_alt_frame());
    _breakpoint_nav_index.set_and_save(target_index);
    _breakpoint_valid.set_and_save(1);  // 标记有效

    // 可选：添加日志
    // GCS_SEND_TEXT(MAV_SEVERITY_INFO, "Breakpoint saved to params: WP%u", target_index);
}
```

#### 3.4 修改 resume() - 从参数加载

```cpp
// 文件: libraries/AP_Mission/AP_Mission.cpp
// 位置: resume() 函数开始处

bool AP_Mission::resume()
{
    // ✨ 新增：从参数恢复断点信息
    if (_breakpoint_valid.get() == 1 && _resume_mode == Resume_Mode::RESUME_BREAKPOINT) {
        // 从参数重建 Location
        _breakpoint_loc.lat = _breakpoint_lat.get();
        _breakpoint_loc.lng = _breakpoint_lon.get();
        _breakpoint_loc.set_alt_cm(_breakpoint_alt.get(), 
                                     (Location::AltFrame)_breakpoint_alt_frame.get());
        
        // 重建航点索引
        _breakpoint_nav_cmd.index = _breakpoint_nav_index.get();
        
        // GCS_SEND_TEXT(MAV_SEVERITY_INFO, "Breakpoint loaded from params: WP%u", 
        //               _breakpoint_nav_index.get());
    }

    // 原有的 resume 逻辑...
    if (_flags.state == MISSION_RUNNING) {
        // ...
    }
    // ... 后续代码不变 ...
}
```

#### 3.5 修改 init() - 初始化时加载

```cpp
// 文件: libraries/AP_Mission/AP_Mission.cpp
// 位置: init() 函数末尾

void AP_Mission::init()
{
    // ... 原有初始化代码 ...

    // ✨ 新增：初始化时从参数加载断点
    if (_breakpoint_valid.get() == 1) {
        _breakpoint_loc.lat = _breakpoint_lat.get();
        _breakpoint_loc.lng = _breakpoint_lon.get();
        _breakpoint_loc.set_alt_cm(_breakpoint_alt.get(), 
                                     (Location::AltFrame)_breakpoint_alt_frame.get());
        _breakpoint_nav_cmd.index = _breakpoint_nav_index.get();
        
        // GCS_SEND_TEXT(MAV_SEVERITY_INFO, "Breakpoint restored: WP%u, alt=%dm", 
        //               _breakpoint_nav_index.get(), _breakpoint_alt.get()/100);
    }
}
```

#### 3.6 修改清除断点的函数

```cpp
// 文件: libraries/AP_Mission/AP_Mission.cpp
// 在需要清除断点的地方（如任务完成、reset等）

void clear_breakpoint()
{
    // 清除内存变量
    _breakpoint_loc = Location();
    _breakpoint_nav_cmd = Mission_Command();
    
    // ✨ 清除参数（持久化清除）
    _breakpoint_valid.set_and_save(0);
    // 可选：也清除其他值（节省空间）
    // _breakpoint_lat.set_and_save(0);
    // _breakpoint_lon.set_and_save(0);
    // _breakpoint_alt.set_and_save(0);
}

// 或在 reset() 函数中
void AP_Mission::reset()
{
    // ... 原有代码 ...
    
    // 清除断点参数
    _breakpoint_valid.set_and_save(0);
}
```

---

## 四、参数在地面站中的显示

设置完成后，在地面站参数列表中会看到：

```
MIS_BP_LAT      = 314567890    # 纬度 (31.4567890°)
MIS_BP_LON      = 1214567890   # 经度 (121.4567890°)
MIS_BP_ALT      = 15000        # 高度 150米
MIS_BP_FRAME    = 1            # ABOVE_HOME
MIS_BP_INDEX    = 3            # 航点3
MIS_BP_VALID    = 1            # 有效
```

**特点**：
- ✅ 用户可以在地面站看到断点信息
- ✅ 标记为 `@ReadOnly: True`，防止用户误修改
- ✅ 重启后自动恢复
- ✅ 可以手动设置 `MIS_BP_VALID=0` 来清除断点

---

## 五、优缺点分析

### 优点 ✅

1. **持久化存储**
   - 飞控重启后断点信息不丢失
   - 掉电后也能保留

2. **可视化**
   - 在地面站参数列表中可见
   - 便于调试和验证

3. **手动控制**
   - 可以手动清除断点（设置 `MIS_BP_VALID=0`）
   - 可以查看断点详细信息

4. **实现简单**
   - 利用现有的参数系统
   - 无需额外的存储管理代码

5. **可靠性高**
   - ArduPilot 参数系统久经考验
   - 自动处理 EEPROM 读写

### 缺点 ⚠️

1. **占用参数槽位**
   - 使用 6 个参数槽（索引 4-9）
   - AP_Mission 参数组有限制（通常 64 个槽位）

2. **写入延迟**
   - `set_and_save()` 会写入 EEPROM，有轻微延迟
   - 频繁写入可能影响性能（但断点保存不频繁，影响可忽略）

3. **EEPROM 磨损**
   - 每次保存断点都写入 EEPROM
   - 但现代 Flash 寿命很长（10万次以上），不是问题

---

## 六、替代方案

### 方案 B: 使用 SD 卡存储（适合更复杂的数据）

如果未来需要保存更多信息（如完整的 Mission_Command），可以使用 SD 卡：

```cpp
// 保存到 SD 卡文件
void AP_Mission::save_breakpoint_to_sd()
{
    int fd = AP::FS().open("APM/breakpoint.dat", O_WRONLY | O_CREAT | O_TRUNC);
    if (fd == -1) return;
    
    // 写入断点信息
    AP::FS().write(fd, &_breakpoint_loc, sizeof(_breakpoint_loc));
    AP::FS().write(fd, &_breakpoint_nav_cmd, sizeof(_breakpoint_nav_cmd));
    uint8_t valid = 1;
    AP::FS().write(fd, &valid, sizeof(valid));
    
    AP::FS().close(fd);
}

// 从 SD 卡加载
bool AP_Mission::load_breakpoint_from_sd()
{
    int fd = AP::FS().open("APM/breakpoint.dat", O_RDONLY);
    if (fd == -1) return false;
    
    AP::FS().read(fd, &_breakpoint_loc, sizeof(_breakpoint_loc));
    AP::FS().read(fd, &_breakpoint_nav_cmd, sizeof(_breakpoint_nav_cmd));
    uint8_t valid;
    AP::FS().read(fd, &valid, sizeof(valid));
    
    AP::FS().close(fd);
    return valid == 1;
}
```

**优点**：
- 可以保存更多数据
- 不占用参数槽位
- 写入速度更快

**缺点**：
- 需要 SD 卡支持
- 地面站看不到
- 需要额外的文件管理代码

---

## 七、测试方法

### 7.1 编译测试

```bash
./waf configure --board CubeOrange
./waf copter
```

### 7.2 功能测试

1. **保存断点测试**
   ```
   1. 执行任务到航点3
   2. 切换到 GUIDE 模式
   3. 在地面站查看参数：MIS_BP_VALID 应该为 1
   4. 检查 MIS_BP_INDEX 应该为 3
   ```

2. **重启恢复测试**
   ```
   1. 降落并上锁
   2. 重启飞控（拔电重新上电）
   3. 检查参数：MIS_BP_VALID 仍然为 1
   4. 解锁切换到 AUTO
   5. 应该从断点恢复
   ```

3. **手动清除测试**
   ```
   1. 设置 MIS_BP_VALID = 0
   2. 切换到 AUTO 模式
   3. 应该从头开始任务（如果 MIS_RESTART=1）
   ```

---

## 八、注意事项

1. **参数索引不能冲突**
   - 确保索引 4-9 未被其他参数使用
   - 查看 `AP_Mission::var_info` 确认

2. **向后兼容性**
   - 旧版本升级到新版本时，这些参数默认为 0
   - `_breakpoint_valid=0` 表示无断点，不影响正常功能

3. **参数写入频率**
   - 仅在退出 AUTO 模式时写入
   - 不会频繁写入，无性能问题

4. **调试建议**
   - 初期可以不使用 `@ReadOnly`，方便测试时手动设置参数
   - 稳定后再加上 `@ReadOnly` 防止误操作

---

## 九、完整修改清单

| 文件 | 修改内容 | 行数 |
|------|---------|-----|
| `AP_Mission.h` | 添加 6 个参数声明 | +6 |
| `AP_Mission.cpp` (参数表) | 添加参数定义和注释 | +40 |
| `AP_Mission.cpp` (set_breakpoint) | 添加参数保存代码 | +5 |
| `AP_Mission.cpp` (resume) | 添加参数加载代码 | +10 |
| `AP_Mission.cpp` (init) | 添加初始化加载代码 | +8 |
| `AP_Mission.cpp` (reset) | 添加清除代码 | +1 |

**总计**：约 70 行新增代码

---

## 十、总结

使用参数化方式保存断点信息是完全可行的，而且是**推荐**的实现方式。

**优势**：
- ✅ 简单可靠
- ✅ 持久化存储
- ✅ 地面站可见
- ✅ 易于调试

**建议**：
1. 先实现方案 A（独立参数）
2. 如果未来需要保存更复杂的信息，再考虑 SD 卡方案
3. 添加适当的日志信息辅助调试

这个改进将使您的断点续飞功能达到商用级水平！🚀

