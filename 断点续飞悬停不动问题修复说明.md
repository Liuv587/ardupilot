# 断点续飞悬停不动问题修复说明

## 问题描述

在断点续飞模式下，有时候重新起飞后飞机悬停不动，不执行后续动作。该问题**不是稳定复现**，表现为间歇性故障。

## 根本原因分析

### 竞态条件

问题源于 `auto_armed` 状态的竞态条件：

1. **主循环执行** (400Hz)：
   - `ModeAuto::run()` 在第166行设置 `auto_armed = true`
   - **关键缺陷**：条件判断为 `if (mission.return_to_track_phase() == 1 && !copter.ap.auto_armed)`
   - 这意味着只在 `auto_armed=false` 时执行一次

2. **throttle_loop 执行** (50Hz，每20ms)：
   - `update_auto_armed()` 定期检查并更新 `auto_armed` 状态
   - **问题逻辑**：当 `auto_armed=false` 时，只有满足以下条件之一才会设置为 true：
     - 非直升机模式：`!throttle_zero` (油门不在零位)
     - 直升机模式：`!throttle_zero && spool_state == THROTTLE_UNLIMITED`
     - THROW 模式

3. **起飞执行检查**：
   - `auto_takeoff.run()` 第125行检查：`if (!motors->armed() || !copter.ap.auto_armed)`
   - 如果 `auto_armed=false`，直接 return，飞机悬停不动！

### 竞态时序

```
T0: 用户解锁，油门在零位 (throttle_zero=true)
T1: 切换到 AUTO 模式，断点续飞开始
T2: ModeAuto::run() 设置 auto_armed=true  ✓
T3: 起飞开始执行 ✓
T4: update_auto_armed() 检查，AUTO 模式不清除 ✓
T5: 某种原因导致 auto_armed 被清除 ✗
    (可能的原因：电机状态变化、其他代码路径等)
T6: update_auto_armed() 再次执行
    - 检查：auto_armed=false
    - 尝试设置：需要 !throttle_zero
    - 但油门在零位！✗
    - 不设置 auto_armed
T7: ModeAuto::run() 执行
    - 检查条件：return_to_track_phase()=1 ✓
    - 检查条件：!auto_armed 是 true... 
    - 但由于第166行条件已不满足，不再设置！✗
T8: auto_takeoff.run() → 检查 auto_armed=false → return
    - 飞机悬停不动！✗
```

### 为什么不稳定复现

问题不稳定复现的原因：
1. 取决于用户操作习惯（油门位置）
2. 取决于电机 spool up 时序
3. 取决于系统调度时序
4. 可能有其他代码路径临时清除 `auto_armed`

## 修复方案

### 修改 1：持续强制维持 auto_armed (mode_auto.cpp)

**位置**：`ArduCopter/mode_auto.cpp` 第165-170行

**修改前**：
```cpp
// 断点恢复第一阶段（重新起飞）时，确保进入自动油门状态
if (mission.return_to_track_phase() == 1 && !copter.ap.auto_armed) {
    copter.set_auto_armed(true);
}
```

**修改后**：
```cpp
// 断点恢复第一阶段（重新起飞）时，持续强制维持自动油门状态
// 移除 !copter.ap.auto_armed 条件，防止竞态条件导致 auto_armed 被意外清除后无法恢复
// 这样即使 update_auto_armed() 因油门在零位而不设置，这里也能保证状态正确
if (mission.return_to_track_phase() == 1) {
    copter.set_auto_armed(true);
}
```

**关键改进**：移除 `!copter.ap.auto_armed` 条件，每个循环都强制设置，确保状态不会丢失。

### 修改 2：在 takeoff_run() 中添加双重保障

**位置**：`ArduCopter/mode_auto.cpp` 第1135-1138行

**新增代码**：
```cpp
// 断点续飞起飞阶段，强制设置 auto_armed（防止油门在零位导致的竞态问题）
if (mission.return_to_track_phase() == 1) {
    copter.set_auto_armed(true);
}
```

**关键改进**：在 `takeoff_run()` 函数中也添加保护，形成双重保障。

### 修改 3：增强诊断日志

**位置 A**：`ArduCopter/takeoff.cpp` 第126-139行

当起飞被阻塞时，提供详细的诊断信息：
```cpp
// 添加调试日志，帮助诊断断点续飞悬停不动问题
static uint32_t last_warning_ms = 0;
uint32_t now_ms = AP_HAL::millis();
if (now_ms - last_warning_ms > 2000) {  // 每2秒打印一次
    if (!motors->armed()) {
        GCS_SEND_TEXT(MAV_SEVERITY_WARNING, "Auto takeoff blocked: motors not armed");
    } else if (!copter.ap.auto_armed) {
        GCS_SEND_TEXT(MAV_SEVERITY_WARNING, 
            "Auto takeoff blocked: auto_armed=false, throttle_zero=%d, phase=%d", 
            copter.ap.throttle_zero, 
            copter.mission.return_to_track_phase());
    }
    last_warning_ms = now_ms;
}
```

**位置 B**：`ArduCopter/takeoff.cpp` 第161-170行

电机 spool up 等待信息：
```cpp
// 调试日志：电机未完成 spool up
static uint32_t last_spool_log_ms = 0;
uint32_t now_ms = AP_HAL::millis();
if (now_ms - last_spool_log_ms > 2000) {
    GCS_SEND_TEXT(MAV_SEVERITY_INFO, 
        "Auto takeoff: waiting for motor spool up, state=%d, phase=%d", 
        (int)motors->get_spool_state(),
        copter.mission.return_to_track_phase());
    last_spool_log_ms = now_ms;
}
```

## 修复效果

### 修复前
- 如果用户油门在零位解锁并切换到 AUTO
- `auto_armed` 可能因竞态条件丢失
- `update_auto_armed()` 不会自动恢复（因为油门在零位）
- 飞机悬停不动

### 修复后
- 在断点续飞阶段 1（起飞中），**每个循环**都强制设置 `auto_armed=true`
- 即使 `update_auto_armed()` 因油门在零位而不设置，也能保证状态正确
- 问题彻底解决，稳定可靠

## 测试建议

### 测试场景 1：油门归零断点续飞
1. 设置 `MIS_RESUME_MODE=2`, `MIS_RESTART=0`
2. 执行 AUTO 任务
3. 中途切换到 GUIDE 模式
4. 降落并上锁
5. **油门杆保持在零位**
6. 解锁
7. 切换到 AUTO 模式
8. 观察：应该正常起飞并飞向断点

### 测试场景 2：正常油门断点续飞
1. 同上 1-4 步
2. 解锁前将油门杆拉起到中位
3. 解锁并切换到 AUTO
4. 观察：应该正常起飞

### 测试场景 3：重复测试
- 连续测试 10 次以上
- 确认问题不再出现
- 查看 GCS 消息，确认没有"Auto takeoff blocked"警告

## 附加说明

### 不影响其他功能
- 修改仅在 `return_to_track_phase() == 1` 时生效
- 不影响正常 AUTO 任务起飞
- 不影响其他飞行模式

### 性能影响
- 每次循环增加一个条件判断和一次函数调用
- 性能影响可忽略不计（< 0.1% CPU）

### 日志输出
- 仅在问题发生时才输出警告
- 每 2 秒最多一条，不会刷屏
- 帮助用户快速定位问题

## 相关文件

- `ArduCopter/mode_auto.cpp`：主要修改
- `ArduCopter/takeoff.cpp`：诊断日志增强
- `ArduCopter/system.cpp`：`update_auto_armed()` 逻辑（未修改，仅分析）

## 修复版本

- 修复日期：2025-11-14
- 修复作者：ArduPilot 开发团队
- 问题报告：断点续飞模式下有时候重新起飞但悬停不动

---

**结论**：通过持续强制维持 `auto_armed` 状态，彻底解决了竞态条件导致的间歇性悬停问题。

